**MBTI × 感情分析レコメンド「TYPECAST」を Go / React / Gemini で作った話**

---

## はじめに

「映画を観ようと思ってNetflixを開いたのに、作品を選んでいるうちに疲れて、結局YouTubeを見て寝る。」

……そんな経験、ありませんか。私はあります。ほぼ毎回あります。

「今の自分の気分」と「自分の性格（好み）」をちゃんと理解したうえで、  
**「今日はこれを観て。なぜなら君はいま、こういう状態だから。」**  
と、論理的に説得してくれる存在が欲しい。

その欲求から生まれたのが、**MBTI × 感情分析シネマレコメンドアプリ『TYPECAST』** です。

- **サービスURL**  
  `https://tycast.net/`

- **ざっくり何をするサービスか**
  - MBTI（16タイプ）と、いまのMood（気分・悩み）を入力
  - バックエンドで Gemini が心理機能と心理状態を解釈
  - **「なぜその映画なのか」の理由付きで、映画を3本レコメンド** します

この記事では、現行リポジトリ（Go/Echo + Cloud Run、React/Vite + Firebase Hosting、Firestore、Gemini、TMDB）をベースに、特にハマりやすかった以下のポイントに絞って紹介します。

- **Geminiへの人格埋め込み（プロンプト設計と扱い方）**
- **SPAなのに「動的OGP」を成立させるためのバックエンド構成**
- **Secrets / 環境変数をどう持たせ、デプロイフローに落とし込むか**

ハッカソン向けに「再現しやすい実装」になるよう、できるだけ具体的に書きます。

---

## プロダクトの特徴

TYPECASTは、「当てにいく診断」ではなく、**「納得感のある理由付きレコメンド」** にこだわったサービスです。

- **理由付きレコメンド**
  - 例: 「INTPは抽象概念の整合性や思考実験に快感を得やすい」など  
    MBTIの心理機能（主機能 / 補助機能）を根拠に、**その人に刺さりそうなポイント** を説明します。
- **Moodの感情分析 & スコア化**
  - ユーザーが入力したテキストを Gemini で感情分析し、スコアやカテゴリを算出。
  - 過去1週間分の Mood 推移をチャートで表示し、「最近の自分の状態」を振り返れます。
- **1日N回までの利用制限（β運用）**
  - レコメンドはコストを読みづらいので、`DAILY_RECOMMEND_LIMIT`（デフォルト3〜5回）で制御。
  - ユーザー単位で回数をカウントし、**βテストでも安心して回せる設計** にしています。
- **Xシェア対応の動的OGP**
  - 診断結果をシェアすると、**映画タイトル・Mood・スコア入りのOGP画像** を動的生成。
  - 「結果を貼るだけで、どんな診断だったかが一目で伝わる」体験を狙いました。

---

## 全体アーキテクチャ（モノレポ構成）

リポジトリは、**フロントエンドとバックエンドを同居させたモノレポ構成** です。

- **Frontend**
  - React + Vite + TypeScript
  - Firebase Hosting でホスティング
  - Firestore / Firebase Authentication と連携
- **Backend**
  - Go + Echo
  - Docker コンテナ化 → Cloud Run にデプロイ
- **Auth / DB**
  - Firebase Authentication
  - Firestore（ユーザー、履歴、シェア情報を保存）
- **AI**
  - Gemini API（`gemini-2.0-flash`）
- **Movie Data**
  - TMDB API（作品情報の取得）
- **CI/CD**
  - GitHub Actions
    - Cloud Run へのデプロイ（バックエンド）
    - Firebase Hosting へのデプロイ（フロントエンド）

**1リポジトリで完結** するようにしておくことで、

- Cloud Run / Firebase Hosting の設定を **GitHub Actions に集約** できる
- ハッカソン中でも、「とりあえずこのリポジトリを clone すれば全部追える」状態になる

というメリットがあります。

---

## 技術TIPS①：Geminiに「人格」を埋め込む（プロンプトは環境変数で管理）

TYPECASTの一番のキモは、**「MBTIに基づく納得感のあるコメントを返す」** ことです。

ただ映画を3本返すだけなら任意のLLMでもできますが、TYPECASTでは Gemini に対して

- **心理学と映画理論に詳しいコンシェルジュ**
- **MBTIの心理機能を理解している**
- **レスポンスは JSON だけを喋る**

という人格・役割を与えています。

ここで重要なのが、

- **レスポンス形式を JSON に固定して Go 側で安定してパースすること**
- **そのプロンプト（人格設定）を「コードに直書きしない」こと**

の2点です。

### JSON固定：`ResponseMIMEType` を指定する

Geminiのレスポンスが自然文＋JSONの混ざった形になると、Go でのパースが壊れがちです。

そこで、クライアント生成時に

- **`ResponseMIMEType = "application/json"`**

を設定し、**「JSON以外返さないでね」という約束をAPIレベルで固定** しています。

これだけで、

- 文章のあとにJSONが続いてパーサーが落ちる
- 余計な説明文が混ざる

といった事故がかなり減り、ハッカソンの限られた時間で「LLMまわりのデバッグにハマる」リスクを抑えられました。

### プロンプトは `GEMINI_PROMPT_TEMPLATE` 環境変数で注入する

人格を定義するプロンプトは、リポジトリには置かず

- **`GEMINI_PROMPT_TEMPLATE` という環境変数に格納**
- GitHub Actions（Cloud Run デプロイ時）やローカル `.env` から注入

する方式にしています。

運用のポイントは以下の通りです。

- **改行は `\n` を含む文字列として環境変数に入れる**
  - Go側で `strings.ReplaceAll(template, "\\n", "\n")` して復元
- `fmt.Sprintf` のプレースホルダに差し込む情報
  - MBTIタイプ
  - 心理機能の説明
  - ユーザーのMoodテキスト
  - 除外したい映画タイトル（すでに見た/好みでないなど）
- CI / ローカルで簡単に差し替えられるようにしておく

この形にしておくと、

- ハッカソンの本番前に「プロンプトだけ微調整したい」というとき、
  **コードを一切触らずに、環境変数の更新だけで改善** できる
- プロンプト自体（≒ノウハウ）をリポジトリに晒さずに済む

というメリットがあります。

---

## 技術TIPS②：SPAでも「動的OGP」を成立させる（Bot判定 + OGP HTML + 画像生成）

React/Vite のような SPA だと、通常は

- どのURLにアクセスしても `index.html` を返す
- OGPタグもビルド時に固定

という構成になりがちです。  
しかし、このままだと **X（旧Twitter）のクローラーはJSを実行しないため、OGPが正常に出ません**。

TYPECASTでは、**バックエンド（Go）側でクローラーを検知し、OGPタグ付きのHTMLを返す** ことで解決しました。

### 1) シェアの全体フロー

TYPECASTのシェア機能は、ざっくり以下の流れになっています。

1. フロントで「シェア」ボタン押下  
   → `POST /api/share` に、映画タイトル / Mood / スコアなどを送信
2. バックエンドで Firestore に保存し、短いID（UUIDの先頭8文字など）を発行
3. レスポンスとして `share_url`（例: `https://tycast.net/s/:id`）を返す
4. ユーザーがXにURLを貼る
5. Xなどのクローラーが `GET /s/:id` にアクセス
6. バックエンド側で User-Agent を見て
   - **Botなら**: OGPメタタグを含むHTMLを生成して返す  
     - `twitter:image` には `GET /api/ogp?...` のURLをセット
   - **人間のブラウザなら**: フロントのルートへリダイレクト  
     - 例: `FRONTEND_URL?share_id=:id`

こうすることで、

- クローラー → OGP専用HTML
- 人間 → 通常のSPA

という2経路を、1つの `/s/:id` エンドポイントでさばけるようにしています。

### 2) 簡易なBot判定（User-Agent）

Bot判定は割り切ってシンプルに実装しています。

- `User-Agent` を lower-case に変換
- `twitterbot`, `slackbot-linkexpanding` などの文字列を含むかどうかで判定

ハッカソン用途であれば、**ここまでシンプルでも実用上は十分** でした。

### 3) OGP画像生成エンドポイント（`/api/ogp`）

OGP画像自体は、別エンドポイントで PNG を動的生成しています。

- `GET /api/ogp?title=...&mood=...&score=...`
- レスポンス: `image/png`

実装には **`fogleman/gg`** を使っています。

- GoでCanvasライクに描画できるライブラリ
- テキスト描画・背景・簡単なレイアウトがサクッと書ける

#### ハマりポイント：フォント / 静的アセットはバイナリに自動では含まれない

ローカルでは問題なく動いていたのに、Cloud Run に上げると落ちる──  
TYPECASTでは、この原因が **フォントファイルの読み込み** でした。

- 画像生成時に `assets/fonts/Bold.ttf` を読み込んでいる
- しかし、Dockerイメージに `assets/` をコピーしていないと、本番環境ではファイルが存在しない

そのため、Dockerfileでは

- **ビルドステージ** でGoバイナリを作る
- **実行ステージ** にバイナリ + `assets/` ディレクトリをコピー

という形で、**実行イメージにフォントを同梱** しています。  
OGP付きのサービスをCloud Runで動かす場合は、ここがかなり重要なポイントでした。

---

## 技術TIPS③：Secrets / 環境変数の扱いとデプロイ戦略

最後に、**公開できない情報（APIキー・URLなど）をどう扱うか** についてです。

TYPECASTでは、以下の情報を **GitHub Secrets** に集約し、  
**デプロイ時に環境変数として注入** する方針を徹底しました。

- **Gemini / TMDB のAPIキー**
- **Firebase / Firestore の接続情報（Project ID, DB名）**
- **`FRONTEND_URL` / `API_BASE_URL`**（OGPリンク生成やフロント→API通信に必須）

### バックエンド（Cloud Run）への環境変数注入

GitHub Actions から Cloud Run にデプロイする際、

- `gcloud run deploy ... --set-env-vars ...`

を使って、Secrets から取り出した値を環境変数として渡しています。

特に重要だったのが **`FRONTEND_URL` と `API_BASE_URL` を Cloud Run 側で明示すること** です。

- これをミスると、OGPやリダイレクト用のURLが `localhost` のままになりがち
- 実際に、本番環境でも `localhost` のURLが埋まったOGPを出してしまい、慌てて修正しました

ハッカソンなど短期開発では、**URL系の環境変数はチェックリスト化しておくと安心** です。

### フロントエンド（Vite）の環境変数

Viteは、ビルド時に `VITE_` プレフィックスのついた環境変数を埋め込みます。

- 本番では CI のビルドステップで `VITE_API_URL` などを `env:` に渡す
- Firebase Hosting にデプロイされるバンドル内に、その値が焼き込まれる

ここを間違えると、

- 本番フロントエンドがローカルAPIを叩く
- あるいは存在しないエンドポイントに投げてしまう

といった事故につながるので、  
**「本番用 `VITE_API_URL` はどこを向いているか」をデプロイ前に毎回確認** する運用にしています。

---

## 小ネタ：β運用の「1日N回レコメンド制限」

LLMを使ったレコメンドは、コストもレスポンスタイムも読みづらい部分があります。

TYPECASTではβ運用として、

- `DAILY_RECOMMEND_LIMIT`（デフォルト3回程度）を設ける
- Firebase Authentication のユーザーIDごとに、当日分の利用回数をカウント
- 上限に達したら、その日はレコメンドを締める

というシンプルな仕組みを入れました。

これだけでも

- 「一部のユーザーだけが大量に叩いてしまう」リスクを抑えられる
- ハッカソン本番中にクラウド費用を気にせずに済む

という意味で、**精神衛生にかなり効いた** 仕掛けでした。

---

## 今後の展望

TYPECASTはまだβ的な位置づけですが、今後は以下のような方向で育てていきたいと考えています。

- **フィードバックを学習に活かす（簡易RLHF）**
  - ユーザーの「見たことある」「好みじゃない」といったフィードバックを蓄積
  - それをプロンプトや除外リストに反映し、**“専属コンシェルジュ”感** を強めていく
- **RAGの導入**
  - TMDBから収集した映画情報をベクトル検索可能な形で保持
  - Geminiに「根拠付き」で渡すことで、**幻覚の少ないレコメンド** を目指す
- **VOD連携の強化**
  - 「どこで観れるか？」まで含めて提示し、  
    ユーザーが **“選んだあとに迷わない”** 状態まで導く

---

## まとめ

TYPECASTは、**MBTI（心理機能）と今のMoodテキスト** を入力に、  
Gemini が心理学コンシェルジュとして **理由付きで映画を提案する** サービスです。

実装面で特に効いたポイントは以下の3つでした。

- **人格プロンプトをコードに直書きせず、環境変数テンプレートとして管理**
  - `ResponseMIMEType = "application/json"` でレスポンス形式を固めつつ、  
    `GEMINI_PROMPT_TEMPLATE` を使って高速にプロンプト改善できるようにした。
- **SPAでもOGPを成立させるための「Bot判定 + OGPタグHTML + PNG動的生成」**
  - `/s/:id` でクローラーと人間を分岐し、`/api/ogp` でOGP画像を生成する構成にすることで、  
    Xシェアとの相性を担保した。
- **SecretsをGitHub Actionsから安全に注入し、「本番URLを間違えない」運用に寄せる**
  - `FRONTEND_URL` / `API_BASE_URL` / 各種APIキーをGitHub Secretsで一元管理し、  
    Cloud Run / Vite の両方に正しいURLを渡すフローを作った。

もしこの記事で「LLM × MBTI × 映画レコメンド」の組み立て方にピンと来るところがあれば、  
ぜひ一度触ってみてください。

`https://tycast.net/`

